---
layout: ../layouts/ExhibitLayout.astro
title: "Multicore Processors: Keeping It Cool"
description: "A short description of your topic."
author: "Ang, Byron Scott; Claro, Stephen Jakobb; Placer, Paul John; Sotingco, Kimberly Wynelle; Tee, John Lorens"
readingTime: "6-7 minutes"
---

{/* component imports */}
import InteractiveCpu from '../components/S03_Group6/InteractiveCpu.jsx';
import Background from '../components/S03_Group6/Background.astro';
import ComparisonTable from '../components/S03_Group6/ComparisonTable.jsx';
import TrendGraph from '../components/S03_Group6/TrendGraph.jsx';
import ChefAnalogy from '../components/S03_Group6/ChefAnalogy.jsx';
import PowerFormula from '../components/S03_Group6/PowerFormula.jsx';

<div className="multicore-exhibit">
<Background />

{/* main article */}
## Problem Solving Story
In the past, computer processors only had a single core, which was defined by a single metric: clock speed. Microprocessors possessed only one computational engine which executed instructions sequentially. Computers did exactly what they were told, when they were told. Under a principle known as Dennard scaling, engineers could shrink transistors on a silicon die, allowing them to increase the clock frequency without a corresponding increase in power density.

<TrendGraph client:load />

However, as the demand for complex 3D rendering and real-time computations grew, single-core processors hit a severe bottleneck. To keep up, manufacturers pushed clock speeds higher, which required exponential increases in voltage. This created a thermal density so high that standard air-cooling solutions could no longer prevent the silicon from melting. This physical barrier became known as the **Power Wall problem**.

Around 2005, realizing that a single core could no longer be pushed to higher frequencies without severe overheating, CPU companies like Intel and AMD shifted their approach: putting multiple cores on a single chip.

<ChefAnalogy client:load />

Think of a CPU like a restaurant kitchen. A single-core processor is like having one incredibly fast, highly skilled chef. To complete more orders, you have to force that chef to work faster and faster. Eventually, the chef gets exhausted, the kitchen overheats, and they can still only cook one dish at a time.

A multicore processor changes the layout. Instead of forcing one chef to work at an impossible speed, you hire four chefs. They can work at a comfortable, moderate pace, but because they are working in parallel (cooking four dishes simultaneously), the total output of the kitchen increases dramatically without anyone overheating.

But how did this solve the Power Wall mathematically? It comes down to the physics of dynamic power consumption in CMOS circuits, expressed by the following formula:

<PowerFormula client:load />

Because power consumption scales quadratically with voltage, running a single core at extreme frequencies requires a high operating voltage that causes power and heat to spike exponentially. By splitting a workload across multiple cores, each individual core can run at a significantly lower frequency and a reduced operating voltage. Because of the quadratic relationship, a slight reduction in voltage yields a massive drop in heat and power usage. This allowed computing throughput to increase through parallel execution without exceeding the thermal limits of the silicon.

## The Multicore CPU
The transition to multicore architecture originally took root in heavy-duty enterprise and server spaces, with IBM introducing the first commercial non-embedded dual-core processor, the POWER4, in 2001. However, the architecture truly went mainstream in 2005 when Intel released the Pentium D and AMD launched the Athlon 64 X2. This marked a monumental paradigm shift in software development; programmers could no longer rely on hardware engineers to automatically speed up their applications and instead had to learn how to write multi-threaded code to take advantage of parallel computing. Over the past two decades, this has evolved from simple dual-core setups to complex hybrid architectures (like ARM's big.LITTLE or Intel's Performance/Efficient core splits) found in modern smartphones, laptops, and desktop computers.

A multicore processor functions by dividing software instructions into separate, independent paths of execution called threads. In a symmetric multiprocessing environment, the operating system treats each physical core as a distinct logical entity. When you run an application, the OS schedules different tasks across these cores simultaneously. The cores coordinate with each other using shared memory frameworks and internal communication lines to ensure data stays synchronized without creating computational bottlenecks or data corruption.

<ComparisonTable client:load/>

## Interactive Multicore CPU Model
<InteractiveCpu client:load/>

## How It Works

The Interactive Multicore CPU simulation lets you control how a processor handles computational tasks. You have three main settings to adjust: active cores, clock speed, and cooling capacity. Active cores determine how many of the eight cores of the processor are working on the task. Using more active cores generally improves processing performance, but also increases power consumption and heat output because more of the processor is working simultaneously. Clock speed controls how fast each core runs. Higher clock speeds reduce execution time but significantly increase power consumption. Cooling capacity represents the maximum amount of CPU heat (measured in watts) that the cooling system can dissipate.

The display provides real-time feedback to help you evaluate your choices. You can see the current CPU power consumption (actual power), the estimated execution time, and the status of each core. Green cores are active and running well, neutral cores are inactive, and red cores indicate thermal throttling, where the processor automatically reduces its speed to avoid exceeding the cooling limit. The effective clock shows your actual CPU frequency after any throttling adjustments, while the total energy score is calculated as power multiplied by execution time, representing the total energy required to complete the workload. Your goal is to minimize this score. The most energy-efficient configuration is not always the fastest, so aim for the lowest energy score rather than the shortest execution time.

To succeed, start with moderate settings like four cores at 2.5 GHz with 100W cooling. Adjust one parameter at a time and watch how the feedback changes. If you see red cores, reduce your clock speed or number of active cores to stay within cooling limits. The key lesson is that increasing the clock speed or number of active cores beyond the cooling capacity causes thermal throttling, which actually makes the task take longer. Experiment with different combinations to discover the balance between performance, power consumption, and cooling that minimizes the total energy score.

## References
- https://en.wikipedia.org/wiki/Multi-core_processor
- https://www.cs.utexas.edu/~lin/cs380p/Free_Lunch.pdf
- https://www.edn.com/future-of-computers-part-2-the-power-wall/ 
- https://en.wikipedia.org/wiki/Voltage_and_frequency_scaling
- https://www.sciencedirect.com/topics/computer-science/dynamic-power-consumption
- https://www.sciencedirect.com/topics/computer-science/total-execution-time
- https://76services.co.uk/thermal-throttling/thermal-throttling-vs-power-limit-throttling/
- https://www.sciencedirect.com/topics/computer-science/dynamic-voltage-and-frequency-scaling
- https://www.microchipusa.com/electrical-components/cpu-power-dissipation
- https://www.revisiondojo.com/blog/single-core-vs-multi-core-processors-explained
- https://www.karlrupp.net/2018/02/42-years-of-microprocessor-trend-data/
</div>
